# Vercel Environment Variables Configuration
# Add these to your Vercel project dashboard

## Frontend (.env.production in frontend folder or set in Vercel UI)
VITE_API_BASE_URL=https://datapilot-backend.onrender.com

## Backend CORS Configuration
# The backend should allow the Vercel frontend URL
# Update main.py CORS allow_origins with your actual Vercel URL
